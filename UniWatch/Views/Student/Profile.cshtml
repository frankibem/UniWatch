@model IEnumerable<UniWatch.Models.Student>

<h3></h3>
<br/>

<p>@Html.ActionLink("Back", "Index", "Class", null, new { @class = "btn btn-default" })</p>

<div class="row help-block">
    &nbsp;
    <div class="col-md-4">
        <div class="form-group form-horizontal">
            @using (Ajax.BeginForm(
                new AjaxOptions
                {
                    HttpMethod = "get",
                    InsertionMode = InsertionMode.Replace,
                    UpdateTargetId = "studentList"
                }))
            {
                <div class="input-group">
                    <input type="search" name="searchTerm" class="form-control"/>
                    <div class="input-group-btn">
                        <input type="submit" class="btn btn-default" value="Search"/>
                    </div>
                </div>
            }
        </div>
    </div>
</div>

@Html.Partial("_ProfileSearch", Model)

